# =====================================================================
# Krypton Agent — environment configuration
# Copy this file to `.env` and fill in the values you actually use.
# =====================================================================

# ---- Provider selection -------------------------------------------------
# Allowed: ollama_local | ollama_cloud | openrouter | nvidia
KRYPTON_PROVIDER=ollama_local

# ---- Ollama (local instance) -------------------------------------------
OLLAMA_LOCAL_HOST=http://127.0.0.1:11434
OLLAMA_LOCAL_MODEL=qwen2.5-coder:7b
# Reasoning effort for thinking-capable models (qwen3, deepseek-r1, gpt-oss...)
# one of: none | low | medium | high  (or empty for model default)
OLLAMA_LOCAL_REASONING_EFFORT=

# ---- Ollama Cloud (hosted) ---------------------------------------------
OLLAMA_CLOUD_HOST=https://ollama.com
OLLAMA_CLOUD_API_KEY=
OLLAMA_CLOUD_MODEL=gpt-oss:120b
OLLAMA_CLOUD_REASONING_EFFORT=

# ---- OpenRouter --------------------------------------------------------
OPENROUTER_API_KEY=
OPENROUTER_MODEL=anthropic/claude-sonnet-4

# ---- NVIDIA NIM (free dev API at build.nvidia.com) --------------------
# Grab your key at https://build.nvidia.com (free tier, no rate limit).
NVIDIA_API_KEY=
# Popular reasoning-capable models (see full list at build.nvidia.com):
#   nvidia/nemotron-3-super-120b-a12b             ← reasoning, large MoE (recommended — fast on NIM)
#   moonshotai/kimi-k2.6                          ← Kimi K2.6, agentic
#   qwen/qwen3-coder-480b-a35b-instruct           ← top-tier coding (no reasoning overhead)
#   qwen/qwen3.5-397b-a17b                        ← Qwen3.5 frontier MoE
#   nvidia/llama-3.3-nemotron-super-49b-v1.5      ← reasoning, mid-size
#   deepseek-ai/deepseek-v4-pro                   ← DeepSeek V4 Pro
#   openai/gpt-oss-120b                           ← OpenAI gpt-oss 120B
#   z-ai/glm-5.1                                  ← GLM 5.1
NVIDIA_MODEL=nvidia/nemotron-3-super-120b-a12b
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
# Reasoning effort — leave EMPTY for max model compatibility (plain request that
# works with any NIM model). Set low|medium|high ONLY for reasoning models
# (Nemotron, DeepSeek-R1, gpt-oss); others may return empty answers if forced.
NVIDIA_REASONING_EFFORT=

# ---- Telegram ----------------------------------------------------------
TELEGRAM_BOT_TOKEN=
# Comma-separated numeric user IDs allowed to talk to the bot.
# Anyone else is silently ignored.
TELEGRAM_AUTHORIZED_USER_IDS=

# ---- Web search --------------------------------------------------------
# Optional Tavily key (faster, deeper results). If empty -> DuckDuckGo.
TAVILY_API_KEY=

# ---- Agent runtime -----------------------------------------------------
# Working directory the agent considers "home" for relative paths.
KRYPTON_WORKDIR=C:\Users\YourName\Desktop\Krypton Agent
# Max iterations of the think->act loop per user turn.
KRYPTON_MAX_ITERATIONS=40
# Context window target (in tokens) before compaction kicks in.
KRYPTON_CONTEXT_BUDGET=24000
# Where SQLite memory / patterns DB lives.
KRYPTON_DATA_DIR=C:\Users\YourName\Desktop\Krypton Agent\data
# Timezone (IANA, e.g. Europe/Rome) for daily-scheduled tasks. Default: UTC.
KRYPTON_TIMEZONE=UTC
